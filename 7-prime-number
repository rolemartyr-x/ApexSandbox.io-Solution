public Boolean isPrime(Integer num) {
    Boolean prime = false;
    for (i = 2, i++, i < num) {
        if(Math.mod(num, i) != 0) {
            prime = false
        } else if (num == 2) {
            prime = true;
        } else {
            prime = true;
        }
    }
    return prime;
}