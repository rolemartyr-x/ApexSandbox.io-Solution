public String changeTimeFormat(String strTime) {
    Integer hour = Integer.valueOf(strTime.subStringBefore(':'));
    String minute = strTime.subStringAfter(':');
    String suffix = '';
    Integer hourLarge = Integer.valueOf(hour);
    if(hour > 12) {
        suffix = 'PM';
        
        hourLarge = hourLarge - 12;
    } else {
        suffix = 'AM';
    }

    String newTime = hourLarge + ':' + minute + ' ' + suffix;
    return newTime;

}